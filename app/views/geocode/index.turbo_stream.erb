<%= turbo_stream.update("search_form") do %>
	<% if @geocodes.blank? %>
		<%= render('layouts/errors') %>
		<%= render('geocode/form') %>
	<% else %>
		<%= render('weather/form', geocodes: @geocodes) %>
	<% end %>
<% end %>